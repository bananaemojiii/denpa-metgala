# Mux
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
MUX_ENV_KEY=2i6frola7qrbrrlsec80b1p2b
# Set after creating a live stream in Mux dashboard or via API
MUX_PLAYBACK_ID=

# Kalshi
KALSHI_API_KEY=
KALSHI_API_HOST=https://trading-api.kalshi.com
# Demo: https://demo-api.kalshi.co

# Polymarket (stretch)
POLYMARKET_API_KEY=

# Admin resolution (shared across web + resolve service — set the same value in both)
ADMIN_SECRET=

# UMA resolve service URL (set in apps/web to forward assertions)
UMA_RESOLVE_SERVICE_URL=http://localhost:3001

# UMA / on-chain
UMA_CHAIN=base
BASE_RPC_URL=https://mainnet.base.org
# Asserter wallet — fund with 500 USDC + gas before event
ASSERTER_PRIVATE_KEY=

# AP Wire
AP_WIRE_API_KEY=

# X / Twitter
X_BEARER_TOKEN=
# Comma-separated stream rule IDs to delete on restart (managed automatically)
X_STREAM_RULE_IDS=

# Anthropic (LLM signal extraction)
ANTHROPIC_API_KEY=

# Redis
REDIS_URL=redis://localhost:6379
# Upstash: rediss://:<password>@<host>:6380

# Market config (JSON map: keyword → Kalshi ticker)
# Populated via scripts/seed-markets.ts or manually
MARKET_MAP_PATH=./market-map.json
